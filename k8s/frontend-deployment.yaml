apiVersion: apps/v1
kind: Deployment
metadata:
  name: babelnet-frontend
  namespace: babelnet
  labels:
    app: babelnet-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: babelnet-frontend
  template:
    metadata:
      labels:
        app: babelnet-frontend
    spec:
      containers:
      - name: frontend
        image: oliviaaa77/babelnet-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5 